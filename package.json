{
  "name": "lex-liberatum-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Lex Liberatum Kernels",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy-testnet.js --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy-mainnet.js --network base"
  },
  "devDependencies": {
    "@chainlink/contracts": "^0.6.1",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^4.9.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
